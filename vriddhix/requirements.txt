# VriddhiX runtime dependencies.

SQLAlchemy>=2.0
alembic>=1.13
pydantic>=2.0
PyYAML>=6.0
pandas>=2.0
numpy>=1.26
yfinance>=0.2

# API (Phase 6+)
fastapi>=0.110
uvicorn[standard]>=0.27

# Deployment target. Not installed locally -- see the deployment note in
# README.md; local runs use SQLite through the same portable schema.
# psycopg[binary]>=3.1

# dev
pytest>=8.0
httpx>=0.27          # fastapi.testclient
